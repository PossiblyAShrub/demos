import os

def lookup_executable(name: str, path: str) -> str | None:
    """
    Traverses the directories in `path`, either returns the absolute path to an
    executable with the given `name` in the `path`, or None if no such
    executable exists.
    """
    raise AssertionError("Not yet implemented")
